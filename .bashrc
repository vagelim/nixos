# edit config
alias e='sudo nixos-rebuild edit'
# rebuild and switch
alias b='sudo nixos-rebuild switch'
# rebuild, upgrade, and switch
alias bu='sudo nixos-rebuild switch --upgrade'
# source
alias src='source ~/.bashrc'
